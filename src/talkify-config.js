talkify = talkify || {};
talkify.config = {
    host: 'http://talkify.net',
    useRemoteServices: true,
    ui:
    {
        audioControls: {
            enabled: false,
            container: document.body
        }
    }
}